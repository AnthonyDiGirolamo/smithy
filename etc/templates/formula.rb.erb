class <%= @formula_name %>Formula < Formula
  homepage "<%= @formula_homepage %>"
  url "<%= @formula_url %>"

  def install
    module_list
    system "./configure --prefix=#{prefix}"
    system "make"
    system "make install"
  end
end
